sudo apt install python3-pip
pip install Flask
pip install -r requirements.txt
pip install sqlalchemy
source venv/bin/activate
flask --app KitchenView.py run

start the program by running "make build" and then "make run"
and then put "http://127.0.0.1:8009/" in your browser
